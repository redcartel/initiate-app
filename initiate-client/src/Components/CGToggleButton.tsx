// import { CGButton, CGButtonProps } from "./CGButton";
// import { CGText } from "./CGText";

// export type CGToggleButtonProps = CGButtonProps & { label: string, subtitle: string, toggle: boolean }

// export const CGToggleButton = ({ label, subtitle, toggle, ...props }: CGToggleButtonProps) => {
//     return (
//         <CGButton theme={props.theme} light={toggle ? !props.light : props.light} {...props}>
//             <CGText>{label}</CGText>
//             <CGText className="text-s">{subtitle}</CGText>
//         </CGButton>
//     )
// }